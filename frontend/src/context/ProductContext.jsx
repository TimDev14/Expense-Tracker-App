import { createContext } from 'react'

// TODO(Milestone 2): replace this generic context with signed-in user/session
// state, then add feature-specific state only when it cannot stay local.
export const ProductContext = createContext(null)
