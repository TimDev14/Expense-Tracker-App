# Shared styles

Global styles live in `src/index.css` and application component styles live in `src/App.css` while the interface is small. Move repeated visual patterns into this folder as the design grows.
