export function PasswordHint() {
  // This small component keeps the account requirement consistent wherever passwords appear.
  return <small className="hint">Use at least eight characters.</small>
}
